# Overgreen — Claude Code Instructions

## Development

- Vite dev server handles incremental compilation — do NOT run `npx tsc --noEmit` for routine checks. Only use it when absolutely necessary (e.g. major refactors touching many files).
